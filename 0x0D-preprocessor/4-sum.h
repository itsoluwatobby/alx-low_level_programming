#ifndef SUM_H
#define SUM_H

/*
 * File: 4-sum.h
 * Desc: Header file that defines a macro-like function named
 *       Sum, to compute the addition to two variables.
 */

#define SUM(x, y) ((x) + (y))

#endif
