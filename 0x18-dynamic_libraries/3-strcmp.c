#include "main.h"
#include <string.h>

/**
 * _strcmp - function that compares two strings.
 * @s1: first parameter
 * @s2: second parameter
 * Return: on success(0)
 */

int _strcmp(char *s1, char *s2)
{
	int result;

	result = strcmp(s1, s2);

	return (result);
}
