#include "main.h"
/**
 * add - function that adds two integers and returns the result
 * @x - integer to compute
 * @y - integer to computer
 * @k - sum of x and y
 * Return: k
 */
int add(int x, int y)
{
	int k;

	k = x + y;
	return (k);
}
